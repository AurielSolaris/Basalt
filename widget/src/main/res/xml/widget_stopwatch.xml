<?xml version="1.0" encoding="utf-8"?>
<!--
  STOPWATCH — Arc gauge and elapsed time, with start and lap in the widget.

  minWidth/minHeight are the size the widget is *placed* at; minResize* is how
  small it may then be dragged. updatePeriodMillis is zero on purpose: the
  framework's own repeat is capped at thirty minutes and cannot be aligned to
  the minute, so every Basalt widget is driven by the module's single exact
  alarm instead. See BasaltWidgets.
-->
<appwidget-provider xmlns:android="http://schemas.android.com/apk/res/android"
    android:minWidth="110dp"
    android:minHeight="110dp"
    android:targetCellWidth="2"
    android:targetCellHeight="2"
    android:minResizeWidth="110dp"
    android:minResizeHeight="110dp"
    android:maxResizeWidth="250dp"
    android:maxResizeHeight="250dp"
    android:updatePeriodMillis="0"
    android:resizeMode="horizontal|vertical"
    android:widgetCategory="home_screen"
    android:previewImage="@drawable/widget_preview"
    android:description="@string/widget_stopwatch_description"
    android:initialLayout="@layout/glance_default_loading_layout" />
